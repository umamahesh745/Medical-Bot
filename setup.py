from setuptools import setup, find_packages

setup(
    name="GENERATIVE-AI-PROJECT",  # your package name
    version="0.1",
    packages=find_packages(),
    install_requires=[ 
        # your dependencies
    ],
    # Optional but recommended:
    author="UMA MAHEHSH",
    description="Medical Bot package",
    # etc.
)
